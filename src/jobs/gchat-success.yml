description: >
  Send job success notification.

executor: default

parameters:
  webhook:
    type: string
    description: Webhook URL of Google chat.

steps:
  - checkout
  - gchat-notify:
      webhook: <<parameters.webhook>>
